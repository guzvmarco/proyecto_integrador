# Proyecto_integrador

This repository contains the project files for the integrator project. Key items:

- `3_ml/NaiveBayes_Proyecto_integrador.ipynb` — main Naive Bayes analysis (keep prominent).
- `5_complejidad/Notebook.ipynb` — complexity notebook (keep prominent).
- `3_ml/` — ML scripts and data; intermediate data files have been moved to `3_ml/archive/`.

See `scripts/move_ml_archives.py` for the mover script used to reorganize `3_ml`.

Next steps:

- Initialize a remote on GitHub and push: `git remote add origin <URL>` then `git push -u origin main`.
